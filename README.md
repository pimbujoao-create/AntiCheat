# AntiCheat Android

Sistema de verificação de integridade para clientes Android.

## Como usar (Cliente)

Execute o comando abaixo no Termux:

```bash
pkg install git php android-tools procps -y && rm -rf AntiCheat && git clone https://github.com/SEUUSUARIO/AntiCheat.git && cd AntiCheat && bash run.sh
